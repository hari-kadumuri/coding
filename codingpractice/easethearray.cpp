#include<iostream>
#include<bits/stdc++.h>
using namespace std;
/*
int main() {
    int n;
    cout<<"enter n :\n";
    cin>>n;
    int a[n];
    cout<<"enter array :\n";
    for(int i=0;i<n;i++)
        cin>>a[i];
    for(int i=0;i<=n-2;i++) {
        if(a[i] == a[i+1]) {
            a[i] *= 2;
            a[i+1] = 0;
        }
    }
    int b[n];
    int j=0;
    for(int i=0;i<n;i++) {
        if(a[i])
            b[j++] = a[i];
    }
    for(int i=0;i<j;i++)
        a[i] = b[i];
    for(int i=j;i<n;i++)
        a[i] = 0;
    delete []b;
    cout<<"\nmodified array : ";
    for(int i=0;i<n;i++)
        cout<<a[i]<<" ";
    delete []a;
    return 0;
}
*/
