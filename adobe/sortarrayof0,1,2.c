#include<stdio.h>
/*
int main() {
    int *a, n;
    printf("enter n :\n");
    scanf("%d", &n);
    int i;
    a = (int*)malloc(n*sizeof(int));
    printf("enter array :\n");
    for(i=0;i<n;i++)
        scanf("%d", &a[i]);
    int b[3];
    b[0] = b[1] = b[2] = 0;
    for(i=0;i<n;i++)
        b[a[i]]++;

    for(i=0;i<b[0];i++)
        a[i] = 0;
    while(i<b[0]+b[1])
        a[i++] = 1;
    while(i<b[0]+b[1]+b[2])
        a[i++] = 2;

    printf("Sorted Array :  ");
    for(i=0;i<n;i++)
        printf("%d  ", a[i]);
    return 0;
}
*/
